{
  "name": "learner-s-guide",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/prepare-admin-password-ui.mjs && vite",
    "prebuild": "node scripts/prepare-admin-password-ui.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "prettier --check .",
    "format": "prettier --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hookform/resolvers": "^5.2.2",
    "@radix-ui/react-accordion": "^1.2.12",
    "@radix-ui/react-aspect-ratio": "^1.3.1",
    "@radix-ui/avatar": "^1.3.0"
  }
}
