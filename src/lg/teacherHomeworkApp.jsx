import React,{useEffect,useMemo,useState}from"react";
import {C,uid}from"@/lg/data";
import {supabase}from"@/lg/supabase";
import {Card,Badge,Sec,GBtn,Shell,AppBar}from"@/lg/ui";
import {NotifPanel}from"@/lg/panels";
import {THHome,THSchedule,THAttendance}from"@/lg/teacher";
import {T6Materials}from"@/lg/teacherWorkflows";
import {TTests,TTestResults}from"@/lg/teacherTests";

const todayISO=()=>new Date().toISOString().slice(0,10);
const errText=e=>e instanceof Error?e.message:(e?.message||"Something went wrong. Please try again.");
async function loadTeacherProfile(teacherId){const{data,error}=await supabase.from("teachers").select("id,name,tid,subject,phone,classes,status").eq("id",teacherId).maybeSingle();if(error)throw error;return data;}
async function loadTeacherBatches(teacherId){const{data:entries,error}=await supabase.from("timetable_entries").select("batch_id,subject_name").eq("teacher_id",teacherId).eq("status","active");if(error)throw error;const ids=[...new Set((entries||[]).map(x=>x.batch_id).filter(Boolean))];if(!ids.length)return[];const{data:batches,error:be}=await supabase.from("batches").select("id,name,cls,sec,status").in("id",ids);if(be)throw be;const subjects=new Map();for(const e of entries||[]){const k=String(e.batch_id);if(!subjects.has(k))subjects.set(k,new Set());if(e.subject_name)subjects.get(k).add(e.subject_name)}return(batches||[]).filter(b=>b.status==null||b.status==="active").map(b=>({...b,subjects:[...(subjects.get(String(b.id))||new Set())]}));}
const ACCEPT=".pdf,.ppt,.pptx,.doc,.docx,.png,.jpg,.jpeg";
const TYPES=new Set(["application/pdf","application/vnd.ms-powerpoint","application/vnd.openxmlformats-officedocument.presentationml.presentation","application/msword","application/vnd.openxmlformats-officedocument.wordprocessingml.document","image/png","image/jpeg"]);
const validFile=f=>Boolean(f&&((f.type&&TYPES.has(f.type))||/\.(pdf|pptx?|docx?|png|jpe?g)$/i.test(f.name)));
const fileLabel=f=>{if(!f)return"No file selected";const mb=f.size/1048576;return`${f.name} · ${mb<1?(f.size/1024).toFixed(0)+" KB":mb.toFixed(1)+" MB"}`};

export function T5HomeworkWithFiles({teacher}){const[batches,setBatches]=useState([]),[rows,setRows]=useState([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[form,setForm]=useState({batchId:"",subject:"",desc:"",due:"",file:null});const refresh=async()=>{setLoading(true);setError("");try{const[bs,hw]=await Promise.all([loadTeacherBatches(teacher?.id),supabase.from("homework").select("id,batch_id,cls,sec,subject,desc,given,due,tid,pdfname,storage_path,file_size,mime_type,created_at").eq("tid",teacher?.id).order("created_at",{ascending:false})]);if(hw.error)throw hw.error;setBatches(bs);setRows(hw.data||[]);setForm(f=>({...f,batchId:f.batchId&&bs.some(b=>String(b.id)===String(f.batchId))?f.batchId:(bs[0]?.id||""),subject:f.subject&&bs.some(b=>b.subjects.includes(f.subject))?f.subject:(bs[0]?.subjects?.[0]||"")}))}catch(e){setError(errText(e))}finally{setLoading(false)}};useEffect(()=>{if(teacher?.id)void refresh()},[teacher?.id]);const selected=useMemo(()=>batches.find(b=>String(b.id)===String(form.batchId)),[batches,form.batchId]);const chooseFile=e=>{const f=e.target.files?.[0]||null;e.target.value="";if(!f){setForm(x=>({...x,file:null}));return}if(!validFile(f)){setError("Unsupported file. Use PDF, PPT/PPTX, DOC/DOCX, PNG or JPG.");return}if(f.size>50*1024*1024){setError("Maximum attachment size is 50 MB.");return}setError("");setForm(x=>({...x,file:f}))};const save=async()=>{if(!selected||!form.subject||!form.desc.trim()||!form.due){setError("Select a class and subject, then enter homework and a due date.");return}if(!selected.subjects.includes(form.subject)){setError("That subject is not assigned to you for this class.");return}if(form.file&&!validFile(form.file)){setError("Unsupported attachment type.");return}setSaving(true);setError("");let path="",homeworkId=uid(),inserted=false;try{if(form.file){const safe=form.file.name.replace(/[^a-zA-Z0-9._-]/g,"_");path=`teacher/${teacher.id}/${selected.id}/${crypto.randomUUID()}-${safe}`;const{error:e}=await supabase.from("homework").insert({id:homeworkId,batch_id:selected.id,cls:selected.cls||"",sec:selected.sec||"",subject:form.subject,desc:form.desc.trim(),given:todayISO(),due:form.due,tid:teacher.id,storage_path:path,pdfname:form.file.name,file_size:form.file.size,mime_type:form.file.type||"application/octet-stream"});if(e)throw e;inserted=true;const{error:up}=await supabase.storage.from("homework").upload(path,form.file,{upsert:false,contentType:form.file.type||"application/octet-stream"});if(up){await supabase.from("homework").delete().eq("id",homeworkId).eq("tid",teacher.id);throw up}}else{const{error:e}=await supabase.from("homework").insert({id:homeworkId,batch_id:selected.id,cls:selected.cls||"",sec:selected.sec||"",subject:form.subject,desc:form.desc.trim(),given:todayISO(),due:form.due,tid:teacher.id});if(e)throw e;inserted=true}setForm(f=>({...f,desc:"",due:"",file:null}));await refresh()}catch(e){if(path&&!inserted)await supabase.storage.from("homework").remove([path]);setError(errText(e))}finally{setSaving(false)}};const remove=async row=>{if(!window.confirm("Delete this homework?"))return;setError("");const{error:e}=await supabase.from("homework").delete().eq("id",row.id).eq("tid",teacher.id);if(e){setError(errText(e));return}if(row.storage_path){const{error:se}=await supabase.storage.from("homework").remove([row.storage_path]);if(se)setError(errText(se))}await refresh()};return <div><Sec title="Homework 📝"/><Card style={{marginBottom:14}}><div style={{fontWeight:800,color:C.text,marginBottom:10}}>Assign to my class</div>{error&&<div style={{color:C.red,background:"#FFF1F2",padding:10,borderRadius:10,fontSize:12,marginBottom:10}}>{error}</div>}{loading?<div style={{color:C.sub,fontSize:13}}>Loading your classes…</div>:batches.length===0?<div style={{color:C.sub,fontSize:13}}>No active classes are assigned to you yet.</div>:<><select value={form.batchId} onChange={e=>{const b=batches.find(x=>String(x.id)===e.target.value);setForm(f=>({...f,batchId:e.target.value,subject:b?.subjects?.[0]||""}))}} style={{width:"100%",padding:11,borderRadius:10,border:`1px solid ${C.border}`,marginBottom:9}}>{batches.map(b=><option key={b.id} value={b.id}>{b.name} · Class {b.cls||"-"}-{b.sec||"-"}</option>)}</select><select value={form.subject} onChange={e=>setForm({...form,subject:e.target.value})} style={{width:"100%",padding:11,borderRadius:10,border:`1px solid ${C.border}`,marginBottom:9}}>{selected?.subjects?.map(s=><option key={s}>{s}</option>)}</select><textarea value={form.desc} onChange={e=>setForm({...form,desc:e.target.value})} placeholder="Homework description" rows={3} style={{width:"100%",padding:11,borderRadius:10,border:`1px solid ${C.border}`,marginBottom:9,resize:"vertical"}}/><input type="date" value={form.due} onChange={e=>setForm({...form,due:e.target.value})} style={{width:"100%",padding:11,borderRadius:10,border:`1px solid ${C.border}`,marginBottom:9}}/><input type="file" accept={ACCEPT} disabled={saving} onChange={chooseFile} style={{width:"100%",padding:8,borderRadius:10,border:`1px dashed ${C.border}`,marginBottom:6}}/><div style={{fontSize:11,color:C.sub,marginBottom:10}}>{form.file?`📎 ${fileLabel(form.file)}`:"Optional attachment · PDF, PPT/PPTX, DOC/DOCX, PNG/JPG · max 50 MB"}</div><GBtn ch={saving?"Saving…":"Assign Homework ✓"} onClick={save}/></>}</Card><Sec title={`My Homework (${rows.length})`}/>{rows.map(h=><Card key={h.id} style={{marginBottom:10}}><div style={{display:"flex",justifyContent:"space-between",gap:10}}><div><Badge label={h.subject}/><div style={{fontWeight:700,color:C.text,marginTop:5}}>{h.desc}</div><div style={{fontSize:11,color:C.sub,marginTop:5}}>Due: {h.due}</div>{h.pdfname&&<div style={{fontSize:11,color:C.sub,marginTop:5}}>📎 {h.pdfname}</div>}</div><button onClick={()=>void remove(h)} style={{border:0,borderRadius:9,padding:"6px 9px",background:"#FFF1F2",color:C.red,cursor:"pointer"}}>🗑</button></div></Card>)}</div>}

export function TeacherAppWithHomeworkFiles({user,onLogout}){const[tab,setTab]=useState("home"),[showNotif,setShowNotif]=useState(false),[teacher,setTeacher]=useState({id:user.ref,name:user.name,subject:"",classes:[]});useEffect(()=>{let alive=true;void loadTeacherProfile(user.ref).then(t=>{if(alive&&t)setTeacher(t)}).catch(e=>console.error("Unable to load teacher profile:",e));return()=>{alive=false}},[user.ref]);const tabs=[{key:"home",icon:"🏠",label:"Home"},{key:"schedule",icon:"📅",label:"Schedule"},{key:"attendance",icon:"✅",label:"Attend."},{key:"homework",icon:"📝",label:"HW"},{key:"tests",icon:"📋",label:"Tests"},{key:"materials",icon:"📚",label:"Notes"}];const content=tab==="home"?<THHome teacher={teacher}/>:tab==="schedule"?<THSchedule teacher={teacher}/>:tab==="attendance"?<THAttendance teacher={teacher}/>:tab==="homework"?<T5HomeworkWithFiles teacher={teacher}/>:tab==="tests"?<><TTests teacher={teacher}/><TTestResults teacher={teacher}/></>:<T6Materials teacher={teacher}/>;return <><Shell header={<AppBar name={user.name} role="teacher" userId={user.id} onLogout={onLogout} onNotif={()=>setShowNotif(true)}/>} tabs={tabs} activeTab={tab} setTab={setTab}>{content}</Shell>{showNotif&&<NotifPanel userId={user.id} onClose={()=>setShowNotif(false)}/>}</>}
