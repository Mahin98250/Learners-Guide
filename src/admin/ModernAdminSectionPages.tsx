import { useEffect, useMemo, useState } from "react";
import { supabase } from "@/lg/supabase";
import "@/admin/modern-admin-sections.css";

type Row = Record<string, any>;
type Section = "attendance" | "results" | "marks" | "fees" | "accounts" | "profiles" | "analytics";
type Props = { section: Section; onBack?: () => void };
type InstituteData = { students: Row[]; teachers: Row[]; batches: Row[]; attendance: Row[]; fees: Row[]; homework: Row[]; announcements: Row[]; tests: Row[]; results: Row[]; loading: boolean; error: string };
const clean = (v: any) => String(v ?? "").trim();
const lower = (v: any) => clean(v).toLowerCase();
const amount = (v: any) => Number(v || 0);
const pct = (n: number, d: number) => d ? Math.round((n / d) * 100) : null;
const fmtDate = (v: any) => { const s = clean(v); if (!s) return "—"; const d = new Date(s); return Number.isNaN(d.getTime()) ? s : d.toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" }); };
function useInstituteData(): InstituteData { const initial: InstituteData = { students: [], teachers: [], batches: [], attendance: [], fees: [], homework: [], announcements: [], tests: [], results: [], loading: true, error: "" }; const [data, setData] = useState<InstituteData>(initial); useEffect(() => { let live = true; (async () => { const queries = await Promise.all([supabase.from("students").select("id,name,sid,cls,sec,parentname,parentphone,phone,status,enroll,created_at"), supabase.from("teachers").select("id,name,tid,subject,phone,classes,status,created_at"), supabase.from("batches").select("id,name,cls,sec,status,active,created_at"), supabase.from("attendance").select("id,sid,date,status,by,created_at,leave_request_id").order("date", { ascending: false }), supabase.from("fees").select("id,sid,desc,amount,due,status,paidon,created_at").order("created_at", { ascending: false }), supabase.from("homework").select("id,tid,cls,sec,subject,due,created_at,batch_id").order("created_at", { ascending: false }), supabase.from("announcements").select("id,title,target,date,created_at").order("created_at", { ascending: false }), supabase.from("tests").select("id,title,subject,cls,sec,test_date,total_marks,batch_id,created_at").order("test_date", { ascending: false }), supabase.from("test_results").select("id,test_id,student_id,marks,remarks,created_at").order("created_at", { ascending: false })]); if (!live) return; const error = queries.find(q => q.error)?.error?.message || ""; setData({ students: queries[0].data || [], teachers: queries[1].data || [], batches: queries[2].data || [], attendance: queries[3].data || [], fees: queries[4].data || [], homework: queries[5].data || [], announcements: queries[6].data || [], tests: queries[7].data || [], results: queries[8].data || [], loading: false, error }); })(); return () => { live = false; }; }, []); return data; }
function Header({ eyebrow, title, description, onBack }: { eyebrow: string; title: string; description: string; onBack?: () => void }) { return <div className="mas-header"><div><span className="mas-eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{onBack && <button className="mas-secondary" onClick={onBack}>← Dashboard</button>}</div>; }
function Metric({ label, value, hint, tone = "blue" }: { label: string; value: React.ReactNode; hint?: string; tone?: string }) { return <div className={`mas-metric ${tone}`}><span>{label}</span><strong>{value}</strong>{hint && <small>{hint}</small>}</div>; }
function Panel({ title, subtitle, children }: { title: string; subtitle?: string; children: React.ReactNode }) { return <section className="mas-panel"><div className="mas-panel-head"><div><h2>{title}</h2>{subtitle && <p>{subtitle}</p>}</div></div>{children}</section>; }
function Empty({ text = "No records available yet." }: { text?: string }) { return <div className="mas-empty">{text}</div>; }
function AttendancePage({ d, onBack }: { d: InstituteData; onBack?: () => void }) { const present = d.attendance.filter(a => lower(a.status) === "present").length; const absent = d.attendance.filter(a => lower(a.status) === "absent").length; const leave = d.attendance.filter(a => ["leave", "on leave"].includes(lower(a.status))).length; const rate = pct(present, d.attendance.length); const rows = useMemo(() => d.students.map((s: Row) => { const records = d.attendance.filter((a: Row) => String(a.sid) === String(s.id) || String(a.sid) === String(s.sid)); const p = records.filter((a: Row) => lower(a.status) === "present").length; return { ...s, records: records.length, present: p, rate: pct(p, records.length) } as Row; }), [d.students, d.attendance]); return <><Header eyebrow="Academic · Attendance" title="Attendance Center" description="A complete view of attendance health and student-level records." onBack={onBack}/><div className="mas-metrics"><Metric label="Attendance rate" value={rate == null ? "—" : `${rate}%`} hint={`${d.attendance.length} total records`} tone="green"/><Metric label="Present" value={present}/><Metric label="Absent" value={absent} tone="red"/><Metric label="Leave" value={leave} tone="amber"/></div><Panel title="Student attendance" subtitle="Attendance records by learner"><div className="mas-table-wrap"><table><thead><tr><th>Student</th><th>Roll No</th><th>Class</th><th>Records</th><th>Present</th><th>Rate</th></tr></thead><tbody>{rows.map((s: Row) => <tr key={s.id}><td><strong>{s.name || "Unnamed"}</strong></td><td>{s.sid || "—"}</td><td>{s.cls || "—"}-{s.sec || "—"}</td><td>{s.records}</td><td>{s.present}</td><td>{s.rate == null ? "—" : `${s.rate}%`}</td></tr>)}</tbody></table></div>{!rows.length && <Empty text="No students found."/>}</Panel></>; }
function ResultsPage({ d, onBack, marksOnly = false }: { d: InstituteData; onBack?: () => void; marksOnly?: boolean }) { const enriched: Row[] = useMemo(() => d.results.map((r: Row) => { const test = d.tests.find((t: Row) => String(t.id) === String(r.test_id)); const total = amount(test?.total_marks) || 100; return { ...r, test, student: d.students.find((s: Row) => String(s.id) === String(r.student_id)), percentage: Math.round(amount(r.marks) / total * 100) } as Row; }).filter((x: Row) => x.test), [d.results, d.tests, d.students]); const average = enriched.length ? Math.round(enriched.reduce((n, x) => n + Number(x.percentage || 0), 0) / enriched.length) : null; const passed = enriched.filter(x => x.percentage >= 40).length; return <><Header eyebrow={`Academic · ${marksOnly ? "Marks Overview" : "Student Results"}`} title={marksOnly ? "Marks & Performance Overview" : "Student Results Center"} description="Review tests, subject performance, and individual scores." onBack={onBack}/><div className="mas-metrics"><Metric label="Average score" value={average == null ? "—" : `${average}%`} tone="purple"/><Metric label="Pass rate" value={enriched.length ? `${Math.round(passed / enriched.length * 100)}%` : "—"} tone="green"/><Metric label="Tests" value={d.tests.length}/><Metric label="Results entered" value={d.results.length} tone="amber"/></div><Panel title="Latest results" subtitle="Student names and roll numbers are shown; internal IDs stay hidden"><div className="mas-table-wrap"><table><thead><tr><th>Student</th><th>Roll No</th><th>Test</th><th>Subject</th><th>Date</th><th>Score</th><th>Remark</th></tr></thead><tbody>{enriched.slice(0, 80).map((x: Row) => <tr key={x.id}><td><strong>{x.student?.name || "Unknown student"}</strong></td><td>{x.student?.sid || "—"}</td><td>{x.test?.title || "Untitled test"}</td><td>{x.test?.subject || "—"}</td><td>{fmtDate(x.test?.test_date)}</td><td>{amount(x.marks)}/{amount(x.test?.total_marks) || 100} · {x.percentage}%</td><td>{x.remarks || "—"}</td></tr>)}</tbody></table></div>{!enriched.length && <Empty/>}</Panel></>; }
function FeesPage({ d, onBack }: { d: InstituteData; onBack?: () => void }) { const paid = d.fees.filter(f => lower(f.status) === "paid").reduce((n, f) => n + amount(f.amount), 0); const pending = d.fees.filter(f => lower(f.status) === "pending").reduce((n, f) => n + amount(f.amount), 0); const overdue = d.fees.filter(f => lower(f.status) === "overdue").reduce((n, f) => n + amount(f.amount), 0); return <><Header eyebrow="Operations · Fees" title="Fees & Collections" description="Track collection, pending dues, and overdue amounts." onBack={onBack}/><div className="mas-metrics"><Metric label="Collected" value={`₹${paid.toLocaleString("en-IN")}`} tone="green"/><Metric label="Pending" value={`₹${pending.toLocaleString("en-IN")}`} tone="amber"/><Metric label="Overdue" value={`₹${overdue.toLocaleString("en-IN")}`} tone="red"/><Metric label="Total records" value={d.fees.length}/></div><Panel title="Collection health" subtitle="Current fee status mix"><div className="mas-metrics"><Metric label="Paid records" value={d.fees.filter(f => lower(f.status) === "paid").length} tone="green"/><Metric label="Pending records" value={d.fees.filter(f => lower(f.status) === "pending").length} tone="amber"/><Metric label="Overdue records" value={d.fees.filter(f => lower(f.status) === "overdue").length} tone="red"/></div></Panel><Panel title="Outstanding fees" subtitle="Unpaid and overdue student records"><div className="mas-table-wrap"><table><thead><tr><th>Student</th><th>Roll No</th><th>Description</th><th>Amount</th><th>Due</th><th>Status</th></tr></thead><tbody>{d.fees.filter(f => lower(f.status) !== "paid").slice(0, 80).map((f: Row) => { const s = d.students.find((x: Row) => String(x.id) === String(f.sid) || String(x.sid) === String(f.sid)); return <tr key={f.id}><td><strong>{s?.name || "Unknown student"}</strong></td><td>{s?.sid || "—"}</td><td>{f.desc || "Fee"}</td><td>₹{amount(f.amount).toLocaleString("en-IN")}</td><td>{fmtDate(f.due)}</td><td>{f.status || "Unknown"}</td></tr>; })}</tbody></table></div></Panel></>; }
function AccountsPage({ d, onBack }: { d: InstituteData; onBack?: () => void }) { const activeStudents = d.students.filter(s => !s.status || !["inactive", "disabled"].includes(lower(s.status))).length; const activeTeachers = d.teachers.filter(t => !t.status || !["inactive", "disabled"].includes(lower(t.status))).length; return <><Header eyebrow="People · Accounts" title="User Accounts & Access" description="Student and teacher account directory." onBack={onBack}/><div className="mas-metrics"><Metric label="Student accounts" value={d.students.length} hint={`${activeStudents} active`}/><Metric label="Teacher accounts" value={d.teachers.length} hint={`${activeTeachers} active`} tone="purple"/><Metric label="Active people" value={activeStudents + activeTeachers} tone="green"/></div><Panel title="Accounts" subtitle="Current student and teacher records"><div className="mas-table-wrap"><table><thead><tr><th>Person</th><th>Role</th><th>Identifier</th><th>Phone</th><th>Status</th></tr></thead><tbody>{d.students.slice(0, 100).map((s: Row) => <tr key={`s-${s.id}`}><td>{s.name || "Unnamed"}</td><td>Student</td><td>{s.sid || "—"}</td><td>{s.phone || s.parentphone || "—"}</td><td>{s.status || "Active"}</td></tr>)}{d.teachers.slice(0, 100).map((t: Row) => <tr key={`t-${t.id}`}><td>{t.name || "Unnamed"}</td><td>Teacher</td><td>{t.tid || "—"}</td><td>{t.phone || "—"}</td><td>{t.status || "Active"}</td></tr>)}</tbody></table></div></Panel></>; }
function ProfilesPage({ d, onBack }: { d: InstituteData; onBack?: () => void }) { return <><Header eyebrow="People · Profiles" title="Search Profiles" description="Search and review core student and teacher profile information without exposing internal database identifiers." onBack={onBack}/><div className="mas-metrics"><Metric label="Students" value={d.students.length}/><Metric label="Teachers" value={d.teachers.length} tone="purple"/><Metric label="Batches" value={d.batches.length} tone="green"/></div><Panel title="Student profiles" subtitle="Profile directory"><div className="mas-table-wrap"><table><thead><tr><th>Name</th><th>Roll No</th><th>Class</th><th>Section</th><th>Parent / Guardian</th></tr></thead><tbody>{d.students.slice(0, 100).map((s: Row) => <tr key={s.id}><td><strong>{s.name || "Unnamed"}</strong></td><td>{s.sid || "—"}</td><td>{s.cls || "—"}</td><td>{s.sec || "—"}</td><td>{s.parentname || "—"}</td></tr>)}</tbody></table></div></Panel></>; }
function AnalyticsPage({ d, onBack }: { d: InstituteData; onBack?: () => void }) { const attendanceRate = pct(d.attendance.filter(a => lower(a.status) === "present").length, d.attendance.length); const paid = d.fees.filter(f => lower(f.status) === "paid").reduce((n, f) => n + amount(f.amount), 0); const scores = d.results.map((r: Row) => { const t = d.tests.find((x: Row) => String(x.id) === String(r.test_id)); return amount(t?.total_marks) ? amount(r.marks) / amount(t?.total_marks) * 100 : null; }).filter((x): x is number => typeof x === "number" && Number.isFinite(x)); const academic = scores.length ? Math.round(scores.reduce((a, b) => a + b, 0) / scores.length) : null; return <><Header eyebrow="Institute · Analytics" title="Institute Analytics" description="Executive summary of academic, attendance, and operations health." onBack={onBack}/><div className="mas-metrics"><Metric label="Students" value={d.students.length}/><Metric label="Teachers" value={d.teachers.length} tone="purple"/><Metric label="Attendance" value={attendanceRate == null ? "—" : `${attendanceRate}%`} tone="green"/><Metric label="Academic average" value={academic == null ? "—" : `${academic}%`}/><Metric label="Fees collected" value={`₹${paid.toLocaleString("en-IN")}`} tone="amber"/></div><div className="mas-grid two"><Panel title="Academic health" subtitle="Current result records"><div className="mas-metrics"><Metric label="Tests" value={d.tests.length}/><Metric label="Results" value={d.results.length}/></div></Panel><Panel title="Operations health" subtitle="Current operational records"><div className="mas-metrics"><Metric label="Homework" value={d.homework.length}/><Metric label="Announcements" value={d.announcements.length}/></div></Panel></div></>; }
export function ModernAdminSectionPage({ section, onBack }: Props) { const d = useInstituteData(); if (d.loading) return <div className="mas-page"><Header eyebrow="Admin" title="Loading section" description="Preparing the latest institute records." onBack={onBack}/></div>; if (d.error) return <div className="mas-page"><Header eyebrow="Admin" title="Unable to load section" description={d.error} onBack={onBack}/></div>; const page = section === "attendance" ? <AttendancePage d={d} onBack={onBack}/> : section === "results" ? <ResultsPage d={d} onBack={onBack}/> : section === "marks" ? <ResultsPage d={d} onBack={onBack} marksOnly/> : section === "fees" ? <FeesPage d={d} onBack={onBack}/> : section === "accounts" ? <AccountsPage d={d} onBack={onBack}/> : section === "profiles" ? <ProfilesPage d={d} onBack={onBack}/> : <AnalyticsPage d={d} onBack={onBack}/>; return <div className="mas-page">{page}</div>; }
