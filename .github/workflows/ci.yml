name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      # Validate the committed source before any working-tree formatting.
      # This keeps production contracts aligned with what is actually reviewed.
      - name: Production contract checks
        run: node scripts/production-contract-check.mjs

      - name: Audit regression tests
        run: npm test

      - name: ESLint
        run: npm run lint:eslint

      - name: Typecheck
        run: npm run typecheck

      - name: Production build
        run: npm run build

      - name: Performance budget
        run: npm run performance:budget

      # Formatting is intentionally last so tests and build validate the
      # committed source instead of a CI-mutated working tree.
      - name: Format changed source files
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
          else
            BASE_SHA="${{ github.event.before }}"
            if [[ "$BASE_SHA" == "0000000000000000000000000000000000000000" ]]; then
              BASE_SHA="HEAD^"
            fi
          fi

          mapfile -t CHANGED_FILES < <(git diff --name-only "$BASE_SHA" "$GITHUB_SHA" -- \
            '*.js' '*.jsx' '*.ts' '*.tsx' '*.json' '*.css' '*.md' '*.yml' '*.yaml' \
            ':!package-lock.json' ':!.github/workflows/*')
          FILES=()
          for file in "${CHANGED_FILES[@]}"; do
            if [[ -f "$file" ]]; then
              FILES+=("$file")
            else
              echo "Skipping deleted file: $file"
            fi
          done

          if (( ${#FILES[@]} == 0 )); then
            echo "No existing changed files require Prettier formatting."
          else
            printf 'Formatting %s existing changed file(s) with Prettier:\n' "${#FILES[@]}"
            printf ' - %s\n' "${FILES[@]}"
            npx prettier --write "${FILES[@]}"
          fi
